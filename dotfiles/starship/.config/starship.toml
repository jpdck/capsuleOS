# Starship Pills Theme - Dracula Pro Colors
# Based on official Dracula Pro XML for perfect consistency across development environment

add_newline = false
palette = "dracula_pro"

format = """
[╭─](fg:separator)\
$status\
$directory\
$git_branch\
$git_status\
$git_metrics\
$conda\
$rust\
$python\
$nodejs\
$package\
$container\
$docker_context\
$memory_usage\
$battery\
$cmd_duration\
$time\
$line_break\
[╰](fg:separator)\
$character\
"""

[palettes.dracula_pro]
# Core UI colors from official Dracula Pro XML
prompt_ok  = "#8AFF80"   # green - for success
prompt_err = "#F83333"   # red - for errors
icon       = "#22212C"   # background - for icon backgrounds
separator  = "#454158"   # selection - for separators
background = "#22212C"   # background

# Semantic colors using official Dracula Pro palette
directory   = "#80FFEA"  # cyan - for paths/directories
duration    = "#E6DB74"  # filename color from XML - for durations
status      = "#F83333"  # red - for error status
git_branch  = "#9580FF"  # purple - for git branches
git_status  = "#8AFF80"  # green - for git status
git_metrics = "#FFCA80"  # orange - for git metrics
rust        = "#FFCA80"  # orange - consistent with git metrics
python      = "#FFFF80"  # yellow - string color from XML
nodejs      = "#8AFF80"  # green - function color from XML
conda       = "#80FFEA"  # cyan - type color from XML
container   = "#FF80BF"  # pink - keyword color from XML
docker      = "#66d9ef"  # cyan-alt - library function color from XML
memory      = "#FFCA80"  # orange - parameter color from XML
battery     = "#8AFF80"  # green - success color

[character]
success_symbol = "[](fg:prompt_ok)"
error_symbol   = "[](fg:prompt_err)"

[status]
format = "[](fg:status)[$status](bg:status)[](fg:status bg:background)"
pipestatus = true
pipestatus_separator = ""
pipestatus_segment_format = "$status"
pipestatus_format = "[](fg:status)[$pipestatus](bg:status)[](fg:status bg:background)"
disabled = false

[directory]
format = "[](fg:directory)[$path](bg:directory)[](fg:directory bg:background)"
truncate_to_repo = false
truncation_length = 0

[directory.substitutions]
"Documents" = "󰈙"
"Downloads" = ""
"Music"     = "󰝚"
"Pictures"  = ""
"Projects"  = "󰲋"
"Desktop"   = ""
"Videos"    = "󰕧"
".config"   = ""
".local"    = ""
".cache"    = ""

[git_branch]
format = "([](fg:git_branch)[ $branch](bg:git_branch)[](fg:git_branch bg:background))"

[git_status]
format = "([](fg:git_status)[$all_status$ahead_behind](bg:git_status)[](fg:git_status bg:background))"
conflicted = "ﲅ"
ahead      = "${count}"
behind     = "${count}"
diverged   = "${ahead_count}${behind_count}"
untracked  = ""
stashed    = ""
modified   = ""
staged     = ""
renamed    = ""
deleted    = ""

[git_metrics]
format = "([](fg:git_metrics)[( $added)](fg:icon bg:git_metrics)[( $deleted)](fg:icon bg:git_metrics)[](fg:git_metrics bg:background))"
added_style   = "fg:icon bg:git_metrics"
deleted_style = "fg:icon bg:git_metrics"
ignore_submodules = true
disabled = false

[conda]
format = "[](fg:conda)[ conda:$environment](bg:conda)[](fg:conda bg:background)"
style = "fg:background bg:conda"

[rust]
format = "[](fg:rust)[ $version](bg:rust)[](fg:rust bg:background)"
symbol = " "
version_format = "${raw}"
style = "fg:background bg:rust"
detect_files = ["Cargo.toml", "Cargo.lock"]
detect_folders = []
detect_extensions = []

[python]
format = "[](fg:python)[ $version](bg:python)[](fg:python bg:background)"
symbol = " "
version_format = "${raw}"
style = "fg:background bg:python"
detect_files = ["requirements.txt", "pyproject.toml", "setup.py", ".python-version"]
detect_folders = []
detect_extensions = []

[nodejs]
format = "[](fg:nodejs)[ $version](bg:nodejs)[](fg:nodejs bg:background)"
version_format = "${raw}"
style = "fg:background bg:nodejs"
detect_files = ["package-lock.json", "yarn.lock"]
detect_folders = ["node_modules"]
detect_extensions = []

[package]
format = "[](fg:nodejs)[ $version](bg:nodejs)[](fg:nodejs bg:background)"
version_format = "${raw}"
style = "fg:background bg:nodejs"

[container]
format = "[](fg:container)[ $name](bg:container)[](fg:container bg:background)"
style = "fg:background bg:container"

[docker_context]
format = "[](fg:docker)[ $context](bg:docker)[](fg:docker bg:background)"
style = "fg:background bg:docker"

[memory_usage]
format = "[](fg:memory)[󰍛 ${ram}](bg:memory)[](fg:memory bg:background)"
style = "fg:background bg:memory"
disabled = false
threshold = 75

[battery]
format = "[](fg:battery)[$percentage $symbol](bg:battery)[](fg:battery bg:background)"
full_symbol        = "󰁹"
charging_symbol    = "󰂄"
discharging_symbol = "󰂃"
unknown_symbol     = "󰂎"
empty_symbol       = "󰁺"

[[battery.display]]
threshold = 20
style = "fg:background bg:prompt_err"

[[battery.display]]
threshold = 60
style = "fg:background bg:battery"

[[battery.display]]
threshold = 70
style = "fg:background bg:battery"

[cmd_duration]
format = "[](fg:duration)[󱐋 $duration](bg:duration)[](fg:duration bg:background)"
min_time = 1000

[time]
format = "[](fg:duration)[󰥔 $time](bg:duration)[](fg:duration bg:background)"
disabled = true
