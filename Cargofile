# Cargofile - Rust crates to install via `cargo install`
# Raw Usage: while read -r crate; do [[ $crate =~ ^#.*$ ]] || [[ -z $crate ]] || cargo install "$crate"; done < Cargofile

# Development Tools (Cargo-only)
cargo-update        # Update installed executables
cargo-expand        # Show macro expansions
cargo-bloat         # Find what takes most space in executable

# Analysis & Metrics (Cargo-only)
cargo-deps          # Visualize dependency graphs
cargo-geiger        # Detect unsafe code usage

# Build & Release Tools (Cargo-only)
cargo-release       # Release automation
cargo-dist          # Build and distribute binaries
cross               # Cross compilation made easy